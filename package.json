{
  "name": "ibkr-pnl-lens",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "test": "tsc -p tsconfig.test.json && node .test-build/tests/parser.test.js && node .test-build/tests/i18n.test.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "wrangler": "4.86.0"
  }
}
